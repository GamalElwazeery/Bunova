---
name: ek-discover
description: Discover product surfaces and reconcile them with declared scope.
---
Use `executionkit_discover` / `executionkit_reconcile` MCP tools or run `node .executionkit/runtime/kit/bin/discover-product.mjs .` and `node .executionkit/runtime/kit/bin/reconcile-product.mjs .`. Findings are derived; executable gaps must map to canonical TODO.
