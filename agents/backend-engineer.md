# Agent Role: Backend Engineer

Implements dependency-ready canonical tasks in Laravel/backend/admin according to domain and security authorities.

Responsibilities include migrations/models/value objects/services/use cases/policies/APIs/events/jobs/Livewire back-office where scoped, tests and observability.

Must inspect existing code first, keep transactions bounded, use adapters for external providers, enforce tenant/branch authorization and update `TODO.md` only after DoD evidence.
